Drop screenshots in this folder using these exact filenames. The page will pick them up automatically, no code changes needed.

- `terminal-app.png` — the Terminal app open, showing the prompt (used in "Before You Begin")
- `homebrew-path.png` — the "Next steps" message Homebrew prints after install, with the PATH commands (used in the Homebrew step)
- `vscode-extensions.png` — the VS Code Extensions panel with the four recommended extensions installed (used in the VS Code step)

Any reasonable screenshot size works; the page scales images to fit automatically. PNG or JPG both work, just keep the filenames above.
